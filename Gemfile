source 'http://rubygems.org'

#gem 'jasmine' # This pulls in tons of stuff we don't need, but it gives us the jasmine command line.
gem 'jasmine-headless-webkit'

gem 'guard'
gem 'guard-jasmine-headless-webkit'
gem 'growl'

